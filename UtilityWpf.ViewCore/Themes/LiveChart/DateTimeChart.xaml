<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

                    xmlns:local="clr-namespace:UtilityWpf.View.LiveChart">

    <!--<Style x:Key="DateTimeChart" TargetType="{x:Type  local:DateTimeChart }">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:DateTimeChart }">
                    <lvc:CartesianChart x:Name="PART_Chart"
                            DisableAnimations="True"

                            DataTooltip="{x:Null}"
                            Zoom="Xy" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
    <!--Series="{Binding Series}"-->
</ResourceDictionary>