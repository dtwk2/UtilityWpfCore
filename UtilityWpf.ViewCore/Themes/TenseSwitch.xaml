<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:wpftoggleswitch="clr-namespace:ToggleSwitch;assembly=ToggleSwitch"
                    xmlns:ue="clr-namespace:UtilityEnum;assembly=UtilityEnum"
                    xmlns:local="clr-namespace:UtilityWpf.View">


    <Style TargetType="{x:Type local:TenseSwitch}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:TenseSwitch}">
                    <Grid x:Name="Grid" Height="50">
                        <Viewbox  >
                            <wpftoggleswitch:HorizontalToggleSwitch x:Name="ToggleSwitch" 
                                                          IsChecked="True"
                                                                 
                                                        UncheckedContent="{Binding Source={x:Static ue:Tense.Past}}"
                                                           CheckedContent="{Binding Source={x:Static ue:Tense.Future}}" >

                              
                            </wpftoggleswitch:HorizontalToggleSwitch>
                        </Viewbox>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>