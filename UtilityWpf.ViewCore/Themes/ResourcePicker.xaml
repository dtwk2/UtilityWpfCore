<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:UtilityWpf.View"
                    xmlns:converter="clr-namespace:UtilityWpf.Converter;assembly=UtilityWpf">

    <Style  TargetType="{x:Type local:ResourcePicker}" BasedOn="{StaticResource {x:Type ComboBox}}">
        <Setter Property="SelectedIndex" Value="0"></Setter>
        <Setter Property="DisplayMemberPath" Value="AbsolutePath"></Setter>
        <Setter Property="ItemTemplate" >
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={x:Static converter:PathToNameConverter.Instance}}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>