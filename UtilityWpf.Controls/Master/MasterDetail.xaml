<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:UtilityWpf.Controls">


    <Style x:Key="NewStyle" TargetType="{x:Type local:MasterDetail}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:MasterDetail}">
                    <ControlTemplate.Resources>
                        <ResourceDictionary Source="/UtilityWpf.Controls;component/DataTemplate/PropertyDataTemplateSelector.xaml" />
                    </ControlTemplate.Resources>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="5" />
                            <ColumnDefinition Width="5*" />
                        </Grid.ColumnDefinitions>     
                        <ContentControl Content="{TemplateBinding Selector}"/>
                        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />
                        <ContentPresenter Grid.Column="2"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource NewStyle}" TargetType="{x:Type local:MasterDetail}" />

</ResourceDictionary>