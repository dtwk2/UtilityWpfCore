<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:conv="clr-namespace:UtilityWpf.Converter;assembly=UtilityWpf"
                    xmlns:local="clr-namespace:UtilityWpf.Controls" 
                    xmlns:dragablz="http://dragablz.net/winfx/xaml/dragablz"
                    xmlns:utilitywpf="clr-namespace:UtilityWpf;assembly=UtilityWpf"  
                    xmlns:dragablz1="clr-namespace:UtilityWpf.Controls.Dragablz">

    <ResourceDictionary.MergedDictionaries>
        <utilitywpf:SharedResourceDictionary Source="/UtilityWpf.Controls;component/Dragablz/DragablzNoteStyle.xaml"></utilitywpf:SharedResourceDictionary>
        <utilitywpf:SharedResourceDictionary Source="/UtilityWpf.Controls;component/Master/MasterControl.xaml"></utilitywpf:SharedResourceDictionary>
    </ResourceDictionary.MergedDictionaries>


    <ControlTemplate x:Key="DotThumbTemplate">
        <Border  Name="OutBox" >
            <Grid >
                <Path   Margin="0,0,0,0"   
                                                            Width="3"
                                                            Height="3"
                                                            Visibility="Visible"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"
                                                            Data="{StaticResource DotGeometry}"
                                                            RenderTransformOrigin="0.5,0.5"
                                                            Stretch="Uniform"
                                                            Stroke="Black"
                                                            StrokeThickness="2">
                    <Path.RenderTransform>
                        <TransformGroup>
                            <TranslateTransform />
                        </TransformGroup>
                    </Path.RenderTransform>
                </Path>
            </Grid>
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Setters>
                        <Setter Property="Background" >
                            <Setter.Value>
                                <SolidColorBrush Color="Gold" Opacity="0.0"></SolidColorBrush>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="CornerRadius" Value="15"></Setter>
                    </Style.Setters>

                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Cursor" Value="Hand" />
                        </Trigger>
                        <EventTrigger RoutedEvent="UIElement.MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                                                            Storyboard.TargetProperty="Background.Opacity"    
                                                                            To="0.5"
                                                                            Duration="0:0:1"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="UIElement.MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                                                            Storyboard.TargetProperty="Background.Opacity"                                                                     
                                                                            To="0.0"
                                                                            Duration="0:0:1"  />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>
    </ControlTemplate>

    <Style x:Key="DragablzNotes2Style" TargetType="dragablz:DragablzItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
        <!--<Setter Property="Width" Value="{Binding RelativeSource={RelativeSource AncestorType=ItemsControl}, Path=ActualWidth, Converter={x:Static conv:DefaultConverter.Instance}}"></Setter>-->
        <Setter Property="Width">
            <Setter.Value>
                <MultiBinding Converter="{x:Static conv:ActualWidthConverter.Instance}">
                    <Binding RelativeSource="{RelativeSource AncestorType=ItemsControl}" Path="ActualWidth" ></Binding>
                    <Binding RelativeSource="{RelativeSource AncestorType=ScrollViewer}" ></Binding>
                </MultiBinding>
            </Setter.Value>
        </Setter>
        <Setter Property="MinWidth" Value="100"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type dragablz:DragablzItem}">
                    <Grid>


                        <dragablz1:ItemContainerBorder
                            IsDragging="{Binding IsDragging, RelativeSource={RelativeSource TemplatedParent}}" 
                            IsSelected="{Binding IsSelected, RelativeSource={RelativeSource TemplatedParent}}"
                            IsSelectable="{Binding IsMouseOver, ElementName=Main_ViewBox}"
                           />

                        <Viewbox x:Name="Main_ViewBox" HorizontalAlignment="Stretch" RenderTransformOrigin=".5, .5" Margin="0.5">

                            <Viewbox.RenderTransform>
                                <ScaleTransform x:Name="ScaleTransform" ScaleX="1" ScaleY="1" />
                            </Viewbox.RenderTransform>
                            <Grid >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <TextBox Margin="2,1,17,2"  Width="200"  
                                         x:Name="PART_TextBox"
                                         HorizontalAlignment="Stretch" FontSize="5"    
                                         TextWrapping="Wrap"
                                         AcceptsReturn="True"           
                                         MinLines="2"  
                                         Grid.Column="1">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox">
                                            <Setter Property="BorderThickness" Value="0"></Setter>
                                            <Setter Property="Background" Value="Transparent"></Setter>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                                <Thumb VerticalAlignment="Center" 
                                   HorizontalAlignment="Left" 
                                   dragablz:DragablzItem.IsCustomThumb="True"
                                   Height="15"
                                   Width="15" 
                                   Template="{StaticResource DotThumbTemplate}">
                                </Thumb>
                            </Grid>
                        </Viewbox>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style 
           TargetType="{x:Type local:MasterNotesItemsControl}" 
           BasedOn="{StaticResource {x:Type dragablz:DragablzItemsControl}}">
        <Style.Setters>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DragablzNotes2Style}"/>

        </Style.Setters>
    </Style>

    <Style  TargetType="{x:Type local:MasterNotesControl}" BasedOn="{StaticResource {x:Type local:MasterControl}}">
        <Setter Property="ButtonTypes" Value="Add,Remove"></Setter>
        <Setter Property="RemoveOrder" Value="Last"></Setter>
        <Setter Property="Background">
            <Setter.Value >
                <VisualBrush
                TileMode="Tile" Viewport="0,0,10,10" 
                ViewportUnits="Absolute" Viewbox="0,0,10,10"
                ViewboxUnits="Absolute">
                    <VisualBrush.Visual>
                        <Canvas>
                            <Rectangle  Width="10" Height="10" />
                            <Path Stroke="WhiteSmoke" Data="M 0 0 l 10 10" />
                        </Canvas>
                    </VisualBrush.Visual>
                </VisualBrush>

            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>