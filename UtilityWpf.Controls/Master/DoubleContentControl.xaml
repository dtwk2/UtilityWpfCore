<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:UtilityWpf.Controls" xmlns:pt="http://propertytools.org/wpf">

    <Style TargetType="{x:Type local:DoubleContentControl}" BasedOn="{StaticResource {x:Type ContentControl}}">
        <Setter Property="Background" Value="Transparent"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:DoubleContentControl}">
                    <DockPanel x:Name="PART_DockPanel" Background="{Binding Background, Source=TemplatedParent}">
                        <WrapPanel x:Name="PART_WrapPanel"/>
                        <pt:DockPanelSplitter x:Name="PART_DockPanelSplitter" Background="Black"></pt:DockPanelSplitter>
                        <ContentPresenter />
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style TargetType="{x:Type local:WrapControl}" BasedOn="{StaticResource {x:Type local:DoubleContentControl}}"/>

</ResourceDictionary>