<UserControl x:Class="UtilityWpf.DemoApp.View.ChartUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UtilityWpf.DemoApp.View"
             xmlns:chart="clr-namespace:UtilityWpf.Chart;assembly=UtilityWpf.Chart"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <ToggleButton Style="{StaticResource MaterialDesignSwitchToggleButton}" x:Name="ToggleButton1" Checked="ToggleButton_Checked" Unchecked="ToggleButton_Checked"></ToggleButton>
        <chart:OxyChartSelector x:Name="Selector"
                                ItemsSource="{StaticResource Characters}" 
                                
                     
                                Grid.Row="1"></chart:OxyChartSelector>
    </Grid>
</UserControl>
