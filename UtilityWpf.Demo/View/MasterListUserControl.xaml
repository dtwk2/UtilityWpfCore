<UserControl
    x:Class="UtilityWpf.DemoApp.View.MasterListUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dragablz="http://dragablz.net/winfx/xaml/dragablz"
    xmlns:local="clr-namespace:UtilityWpf.DemoApp.View"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="http://schemas.utility.com" xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <local:MainViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary>

            <DataTemplate x:Key="Int32Template" DataType="system:Int32">
                <Grid
                                        Width="200"
                                        Height="100"
                                        Tag="{Binding}">
                    <TextBlock
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Text="{Binding}" />
                </Grid>
            </DataTemplate>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/UtilityWpf.Controls;component/Themes/DragablzItem.xaml"></ResourceDictionary>

            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

        
    </UserControl.Resources>
    
    
    <local:AddDragRowControl CommandParameter="{Binding {x:Null}}" Orientation="Vertical" RemoveOrder="Selected" VerticalContentAlignment="Center">
        <i:Interaction.Triggers>
            <i:EventTrigger  EventName="Change" >
                <i:InvokeCommandAction Command="{Binding ChangeCommand}" PassEventArgsToCommand="True"></i:InvokeCommandAction>
            </i:EventTrigger>
        </i:Interaction.Triggers>
        
        <ListBox ItemsSource="{Binding Rows}" SelectedIndex="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:AddDragItemControl CommandParameter="{Binding NewItem}" RemoveOrder="Last" ButtonTypes="Add,Remove"  >
                        <i:Interaction.Triggers>
                            <i:EventTrigger  EventName="Change" >
                                <i:InvokeCommandAction Command="{Binding ChangeCommand}" PassEventArgsToCommand="True"></i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <dragablz:DragablzItemsControl x:Name="DragablzItemsControl" ItemsSource="{Binding Data}" Background="GhostWhite" 
                                                       VerticalAlignment="Center" 
                                                       Margin="10,0"
                                                       ItemContainerStyle="{StaticResource DragablzItemStyle}"
                                                       ItemTemplate="{StaticResource Int32Template}"
                                                       HorizontalAlignment="Left" 
                                                       HorizontalContentAlignment="Left"
                                                       MouseDown="DragablzItemsControl_MouseDown">
                            <dragablz:DragablzItemsControl.PositionMonitor>                               
                                <dragablz:HorizontalPositionMonitor OrderChanged="StackPositionMonitor_OnOrderChanged"/>
                            </dragablz:DragablzItemsControl.PositionMonitor>
                            <dragablz:DragablzItemsControl.ItemsOrganiser>
                                <dragablz:HorizontalOrganiser />
                            </dragablz:DragablzItemsControl.ItemsOrganiser>                   
                        </dragablz:DragablzItemsControl>
                    </local:AddDragItemControl>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </local:AddDragRowControl>
</UserControl>