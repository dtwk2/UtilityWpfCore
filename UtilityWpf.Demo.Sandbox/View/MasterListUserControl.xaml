<UserControl
    x:Class="UtilityWpf.Demo.View.MasterListUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dragablz="http://dragablz.net/winfx/xaml/dragablz"
    xmlns:local="clr-namespace:UtilityWpf.Demo.View"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="http://schemas.utility.com" 
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:system="clr-namespace:System;assembly=System.Runtime"
    xmlns:vm="clr-namespace:UtilityWpf.Demo.Sandbox.ViewModel"
    d:DesignHeight="450"
    d:Background="White"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <local:MainViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary>

            <DataTemplate x:Key="Int32Template" DataType="{x:Type vm:ElementViewModel}">
                <Grid  Width="200"  Height="100" Tag="{Binding}">
                    <TextBox  HorizontalAlignment="Center"   VerticalAlignment="Center"  Text="{Binding Value}" />
                </Grid>
            </DataTemplate>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/UtilityWpf.Controls;component/Themes/DragablzItem.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>


    </UserControl.Resources>


    <local:AddRowControl CommandParameter="{Binding {x:Null}}" Orientation="Vertical" RemoveOrder="Selected" VerticalContentAlignment="Center"
                         ButtonTypes="Remove,MoveDown,MoveUp">
        <i:Interaction.Triggers>
            <i:EventTrigger  EventName="Change" >
                <i:InvokeCommandAction Command="{Binding ChangeCommand}" PassEventArgsToCommand="True"></i:InvokeCommandAction>
            </i:EventTrigger>
        </i:Interaction.Triggers>

        <ListBox x:Name="MainListBox" ItemsSource="{Binding Rows}" SelectedIndex="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:AddDragItemControl CommandParameter="{Binding NewItem}" RemoveOrder="Last" ButtonTypes="Add,Remove"  >
                        <i:Interaction.Triggers>
                            <i:EventTrigger  EventName="Change" >
                                <i:InvokeCommandAction Command="{Binding ChangeCommand}" PassEventArgsToCommand="True"></i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <view:DragablzHorizontalItemsControl x:Name="DragablzItemsControl" 
                                                       ItemsSource="{Binding Data}"                                                       
                                                       VerticalAlignment="Center"                                                 
                                                       ItemContainerStyle="{StaticResource DragablzItemStyle2}"
                                                       ItemTemplate="{StaticResource Int32Template}">
                        </view:DragablzHorizontalItemsControl>
                    </local:AddDragItemControl>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </local:AddRowControl>
</UserControl>